# iOS home dashboard

`HomeDashboardScreen` is snapshot-only: it displays synchronized focus, priority, change,
maintenance/monitoring, status, action, assessment and report reference. Opening it performs
no engine, orchestration, submission or activation mutation. Explicit no-cache, stale
offline and action-required states exist. Live refresh and backend dashboard contract remain
undeployed.

