# Plan comparison

The deterministic comparator reports moved sessions, exercise and dosage changes,
objective changes, and exposure changes with trigger, rationale, and fingerprint. It does
not diff private traces. Load, warnings, competition, and approval diff presentation remain
to be added before coach UI integration.

