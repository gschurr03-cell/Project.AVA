# Competition and taper context

Competitions include date, event, importance, travel and taper priority. Competition day
is protected. V1 records the context and uses conservative scheduling; it does not apply a
universal volume percentage. Future taper rules must use importance, event, phase, recent
load, fatigue, training age and optional historical response with explicit rule provenance.

