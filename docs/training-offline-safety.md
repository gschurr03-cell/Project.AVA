# Offline training safety

Offline validity is bounded. Account/athlete mismatch, a newer revocation version, or
expiry blocks use. Foreground while online requests reconciliation. Pain and stop events
sort ahead of normal adherence events for synchronization. Production still needs signed
packages, encrypted device storage, push-safe notification copy, remote revocation fetch,
and real iOS reconciliation tests.

