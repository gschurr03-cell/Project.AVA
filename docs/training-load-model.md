# Training load model

Load remains multidimensional: neural/mechanical intensity, sprint/high-speed/acceleration
exposure, plyometric contacts, strength sessions, metabolic demand and recovery cost.
V1 categorical intensity values are fixture architecture, not validated athlete dose.
There is no opaque aggregate score. Formula calibration and rolling workload validation
are safety blockers.

