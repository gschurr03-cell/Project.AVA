# Training determinism

Catalog/template candidates use explicit filters, historical-use count and stable ID order.
Canonical SHA-256 fingerprints exclude trace/request timestamps. Tests prove repeatable
fixture output, stable material fingerprints, operational-metadata independence, material
constraint impact, deterministic session selection, immutable revision fingerprints and
closed progression criteria. No LLM or random plan ranking is used.

