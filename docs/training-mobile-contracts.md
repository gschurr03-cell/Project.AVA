# Training mobile contracts

The portable v1 offline package exposes only the active approved plan pointer, current
week/sessions, warnings, competition context, sync/revocation metadata, expiry, and
integrity fingerprint. Mobile may queue bounded adherence, readiness, pain, note-category,
and stop events. It cannot generate or modify programming logic.

