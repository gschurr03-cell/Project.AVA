# iOS projection experience

Projection presentation separates current measurement from backend expected range,
horizon, assumptions, contributors, limiters, uncertainty and confidence. The contract
requires explicit non-guarantee language; a safety validator rejects “guaranteed” output.
No scenarios, race-time promise or extrapolation is created locally.

