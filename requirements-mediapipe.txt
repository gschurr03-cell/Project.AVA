# Python dependencies for the MediaPipe pose runtime (dev/worker only).
# Install into a Python 3 environment:
#
#   pip install -r requirements-mediapipe.txt
#
# Then verify with:  npm run mediapipe:install-check
# 0.10.18 is the newest release with wheels for both Linux amd64 and arm64,
# keeping the worker image reproducible across production and Apple Silicon CI.
mediapipe==0.10.18
opencv-python-headless==4.11.0.86
# Pin MediaPipe's otherwise-unbounded numerical/transitive dependencies so pip
# does not backtrack across incompatible NumPy 2.x/JAX releases during builds.
numpy==1.26.4
jax==0.4.35
jaxlib==0.4.35
opencv-contrib-python==4.11.0.86
