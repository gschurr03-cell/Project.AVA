import Link from "next/link";
import LegalPage from "@/components/LegalPage";
import { VIDEO_RETENTION_POLICY } from "@/lib/beta/config";
export default function RetentionPage(){return <LegalPage title="Video and data retention (draft)"><p>AVA stores uploaded source video because it is required to process the analysis, review timing geometry, and support a user-requested rerun. Completed results and derived artifacts are retained with the session.</p><p>No automatic deletion period is currently configured. Source-video deletion independent of its session is not currently supported. Deleting a session removes its database record and requests removal of its private source video; failed storage cleanup is handled as an operator task.</p><p>AVA does not use beta videos for model training under the current policy. No model-improvement consent or training-data ingestion is enabled.</p><p>Only authorized account owners, restricted operators where necessary, and AVA processing services may access private video. Protected reports do not expose the source-video storage URL.</p><p>Policy version: {VIDEO_RETENTION_POLICY.policyVersion}. This draft requires policy and legal approval before external beta.</p><p><Link href="/account" className="text-[#3b8eff]">Open account and deletion controls</Link></p></LegalPage>}

